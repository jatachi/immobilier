{% extends 'base.html.twig' %}

{% block title %}{{ property.name }}- Schama immobilier{% endblock %}

{% block body %}
    <h1 class="text-center">{{ property.name }}</h1>

    <div id="carouselExample" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
            {% for key,header in property.attachements %}
                <li data-target="#carouselExample" data-slide-to="{{ key}}"
                {% if key == 0 %} class="active">{% endif %}</li>
            {% endfor %}
        </ol>
        <div class="carousel-inner">
            {% for key,header in property.attachements %}
                <div class="carousel-item{% if key == 0 %} active {% endif %}">
                    <img src="/images/property/{{ header.image }}" class="d-block w-100 image-carousel" alt="...">
                </div>
            {% endfor %}
        </div>
        <a class="carousel-control-prev" href="#carouselExample" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExample" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
    
       {# <div id="myCarousel" class="carousel slide" data-ride="carousel">
            <ol class="carousel-indicators">
            {% for key, header in property.attachements %}
                <li data-target="#myCarousel" data-slide-to="{{ key}}"
                {% if key == 0 %} class="active">{% endif %}</li>
            {% endfor %}
            </ol>
            <div class="carousel-inner">
                {% for key, header in property.attachements %}
                    <div class="carousel-item {% if key == 0 %} active {% endif %} image-carousel " style="background: url('/images/property/{{ header.image }}');">
                    </div>
                {% endfor %}
            </div>
            <a class="carousel-control-prev" href="#myCarousel" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#myCarousel" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div> #}
    <div class="container">
        <table class="table">
            <tbody>
                <tr>
                    <th scope="row">Prix</th>
                    <td>{{ property.price /100}} F CFA</td>
                </tr>
                <tr>
                    <th scope="row">Situation</th>
                    <td>{{ property.situation }}</td>
                </tr>
                <tr>
                    <th scope="row">Description</th>
                    <td>{{ property.description}}</td>
                </tr>
                <tr>
                    <th scope="row">Disponibilité du Bien</th>
                    <td>
                        {% if property.isavaible  %}
                            <div class="alert   alert-danger" role="alert">
                                pas disponible
                            </div>
                        {% else %}
                            <div class="alert alert-success" role="alert">
                                disponible
                            </div>
                            
                        {% endif %}   

                    </td>
                </tr>
            </tbody>
        </table>
        {# <div class="row">
            <div class="col-md-3">Prix</div>
            <div class="col-md-5">{{ property.price /100}} F CFA</div>
            <div class="col-md-2">
                {% if property.isavaible  %}
                    pas disponible
                {% else %}
                    disponible
                {% endif %}   
            </div>
        </div> #}
    </div>
    {# <div class="container">
        <div class="row">
            <div class="col-md-8">
                <div class="row">
                    <div class="col-md-3">Situation : </div>
                    <div class="col-md-9">{{ property.situation }}</div>
                </div>
                <div class="row">
                    <div class="col-md-3">Description</div>
                    <div class="col-md-9">{{ property.description}}</div>
                </div>
            </div>
            <div class="col-md-4">

            </div>
        </div>  #}
    </div>
{% endblock %}
